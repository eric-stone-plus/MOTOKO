{
  "id": "R-VULN-REDIRECT-001",
  "name": "Open redirect",
  "category": "vuln",
  "when": {
    "all": [
      {
        "fact": "class",
        "op": "contains",
        "value": "open_redirect"
      }
    ]
  },
  "then": {
    "hypothesis": "Chain to OAuth token leakage (observe Location without following outbound)",
    "actions": [
      {
        "tool": "curl",
        "cmd": "curl -sSI -o /dev/null -w '%{redirect_url}' -H 'User-Agent: {ua}' {url}?redirect={oob}",
        "cmd_stealth": "curl -sSI -m 15 -o /dev/null -w '%{redirect_url}' -H 'User-Agent: {ua}' {url}?redirect={oob}"
      }
    ],
    "on_hit_class": "auth.token_leak",
    "priority": {
      "impact": 0.5,
      "cost": 0.2
    }
  }
}