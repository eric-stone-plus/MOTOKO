{
  "id": "R-TECH-GIT-001",
  "name": "Git exposure",
  "category": "tech",
  "when": {
    "all": [
      {
        "fact": "class",
        "op": "contains",
        "value": "exposure.git"
      }
    ]
  },
  "then": {
    "hypothesis": "Reconstruct .git and scan for secrets",
    "actions": [
      {
        "tool": "git-dumper",
        "cmd": "git-dumper -u '{ua}' -j 1 -t 20 -r 2 {url}/.git {out}"
      },
      {
        "tool": "trufflehog",
        "depends_on": [0],
        "cmd": "trufflehog filesystem {out} --json --no-verification"
      }
    ],
    "on_hit_class": "secret.leak",
    "priority": {
      "impact": 0.85,
      "cost": 0.25
    }
  }
}
