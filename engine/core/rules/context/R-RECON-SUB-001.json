{
  "id": "R-RECON-SUB-001",
  "name": "Subdomain enumeration (subfinder + passive amass)",
  "category": "context",
  "when": {
    "all": [
      {
        "fact": "url",
        "op": "matches",
        "value": "^https?://"
      },
      {
        "fact": "domain_subdomains_enumerated",
        "op": "eq",
        "value": false
      }
    ]
  },
  "then": {
    "hypothesis": "Subdomain enumeration expands the attack surface",
    "actions": [
      {
        "tool": "subfinder",
        "cmd": "subfinder -d {domain} -silent"
      },
      {
        "tool": "amass",
        "cmd": "amass enum -passive -timeout 3 -d {domain}",
        "timeout": 420
      }
    ],
    "on_hit_class": null,
    "priority": {
      "impact": 0.45,
      "cost": 0.35
    }
  }
}
